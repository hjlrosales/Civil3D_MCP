{
  "name": "autodesk-mcp-e2e",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "description": "End-to-end tests: spawn the real Autodesk.MCP.Server binary and drive it over stdio MCP against a protocol-faithful fake bridge.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
