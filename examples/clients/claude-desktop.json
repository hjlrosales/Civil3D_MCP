{
  "$comment": "Claude Desktop: merge the mcpServers block into claude_desktop_config.json (Help > Settings > Developer > Edit Config, or %APPDATA%\\Claude\\claude_desktop_config.json), then restart Claude Desktop.",
  "mcpServers": {
    "autodesk-mcp": {
      "command": "npx",
      "args": ["-y", "autodesk-mcp-server"]
    }
  }
}
