namespace Civil3D.Domain.Query;

/// <summary>The direction of a sort expression.</summary>
public enum SortDirection
{
    /// <summary>Ascending (A→Z, 0→9).</summary>
    Ascending,

    /// <summary>Descending (Z→A, 9→0).</summary>
    Descending,
}
